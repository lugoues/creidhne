.mise/config.local.toml

# Go / release build artifacts
/bin/
/dist/
/gen-systemd-sections

# Pre-migration scratch (not part of the project)
/testing-old/
/repro-pipe-loss-isolate.ts
/nohup.out

# Real-world test projects (separate git repos, never committed from here)
/test-repos/
